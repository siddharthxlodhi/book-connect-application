spring:
  application:
    name: book-connect-api
  profiles:
    active: dev
  servlet:
    multipart:
      max-file-size: 50MB
springdoc:
  default-produces-media-type: application/json


server:
  address: 0.0.0.0  # <-- ADD THIS LINE
  port: 8080        # <-- OPTIONAL, but good to include explicitly
  servlet:
    context-path: /api/v1